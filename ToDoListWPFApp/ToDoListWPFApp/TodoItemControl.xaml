<UserControl x:Class="ToDoListWPFApp.TodoItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="600">

    <UserControl.Resources>
        <!-- Style cho nút icon -->
        <Style TargetType="Button" x:Key="IconButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Hiệu ứng hover cho border -->
        <Style TargetType="Border" x:Key="ItemContainer">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#f3f8ff" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="White" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource ItemContainer}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Checkbox -->
            <CheckBox x:Name="TaskCheckBox"
                      VerticalAlignment="Center"
                      Margin="4"/>

            <!-- Nội dung công việc -->
            <Grid Grid.Column="1" Margin="10,0">
                <!-- TextBlock hiển thị -->
                <TextBlock x:Name="TaskTitle"
                           Text="Nội dung công việc"
                           FontSize="16"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=TaskCheckBox}" Value="True">
                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- TextBox cho chế độ edit -->
                <TextBox x:Name="TaskEditor"
                         Text="{Binding ElementName=TaskTitle, Path=Text, Mode=TwoWay}"
                         FontSize="16"
                         Visibility="Collapsed"
                         VerticalContentAlignment="Center"
                         KeyDown="TaskEditor_KeyDown"
                         LostFocus="TaskEditor_LostFocus"/>
            </Grid>

            <!-- Nút sửa -->
            <Button Grid.Column="2"
                    Style="{StaticResource IconButton}"
                    Margin="5,0,0,0"
                    Click="EditButton_Click">
                <TextBlock Text="✏" FontSize="16"/>
            </Button>

            <!-- Nút xóa -->
            <Button Grid.Column="3"
                    Style="{StaticResource IconButton}"
                    Margin="5,0,0,0"
                    Click="DeleteButton_Click">
                <TextBlock Text="🗑" FontSize="16"/>
            </Button>
        </Grid>
    </Border>
</UserControl>
